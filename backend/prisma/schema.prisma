generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// Models will be added in Phase 1 of implementation.
// See /doc/prisma_schema.txt for the full planned schema.
